# The render itself: the Blender project, the source passes, and the frames.
# Large binaries, and not part of the scheme -- the wallpaper only needs the
# video to exist on disk.
indoor/

# Bytecode cached by importing or compiling the daemon.
__pycache__/
*.pyc
